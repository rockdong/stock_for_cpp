## 1. restart.sh 重写

- [x] 1.1 添加 `set -e` 错误处理机制
- [x] 1.2 实现当前容器状态显示 (`docker-compose ps`)
- [x] 1.3 实现 MySQL 健康检查等待函数（最长 90 秒）
- [x] 1.4 修正端口信息显示（3000=飞书Bot, 3001=API, 80=Web, 8080=Adminer）
- [x] 1.5 添加监控脚本存在性检查和条件启动
- [x] 1.6 添加时间戳输出函数
- [ ] 1.7 测试 restart.sh 基本功能

## 2. update.sh 创建

- [x] 2.1 创建脚本文件并添加 `set -e` 错误处理
- [x] 2.2 实现当前镜像版本显示函数（显示 image ID 和创建时间）
- [x] 2.3 实现服务停止流程 (`docker-compose down`)
- [x] 2.4 实现镜像拉取流程 (`docker-compose pull`) 及失败处理
- [x] 2.5 实现版本对比显示功能（标记变化镜像）
- [x] 2.6 实现服务启动和 MySQL 健康检查等待
- [x] 2.7 实现悬空镜像清理 (`docker image prune -f`)
- [x] 2.8 添加监控脚本条件启动
- [x] 2.9 添加最终状态显示和访问地址
- [ ] 2.10 测试 update.sh 基本功能

## 3. 验证与测试

- [ ] 3.1 测试 restart.sh：停止 → 启动 → 健康检查 → 状态显示
- [ ] 3.2 测试 update.sh：版本显示 → 拉取 → 启动 → 清理
- [ ] 3.3 测试监控脚本不存在时的静默跳过行为
- [ ] 3.4 测试 Docker Hub 连接失败的错误处理
- [ ] 3.5 确认两个脚本的输出格式一致性